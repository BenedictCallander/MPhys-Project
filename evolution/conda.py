print("Conda Started: CD to Relevant directory --> ")

'''
with h5py.File(cutpath,'r') as f:
            sfr = f['PartType0']['StarFormationRate'][:]
            co_ords = f['PartType0']['Coordinates'][:]
            hcoldgas  = np.where( (sfr > 0.0))[0]
            #dx = f['PartType0']['Coordinates'][:,0] 
            #dy = f['PartType0']['Coordinates'][:,1]
            #dz = f['PartType0']['Coordinates'][:,2]
            pgas_m = f['PartType0']['Masses'][:]
            pgas_met = f['PartType0']['GFM_Metallicity'][:]
            pgas_sfr = f['PartType0']['StarFormationRate'][:]


'''